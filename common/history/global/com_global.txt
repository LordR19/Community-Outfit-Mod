
    #############
    # By Lord R #
    #############
	
# This is executed last among all history
GLOBAL = {
	random_country = {
		set_variable = RRK_preserve_monarchy
		remove_variable = RRK_preserve_monarchy #Used to prevent errors from variable spam
	}
}
