# This is executed last among all history
GLOBAL = {
	If = { #Used to prevent errors from variable spam
		limit = { RRK_is_active_trigger = no }
		random_country = {
			set_variable = RRK_preserve_monarchy
			remove_variable = RRK_preserve_monarchy
		}
	}
}
